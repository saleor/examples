# Saleor Similar Products App Configuration

# Optional: OpenAI API key for generating embeddings
# If not provided, the app will use deterministic mock vectors for testing
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Fallback credentials for seed script (if app not installed yet)
# After app installation, the seed script uses APL authentication automatically
SALEOR_API_URL=https://your-store.saleor.cloud/graphql/
SALEOR_TOKEN=your-api-token-here

# Optional: Authentication storage method
# Default: file (stores in .auth-data.json, good for development)
# Production: upstash (requires UPSTASH_URL and UPSTASH_TOKEN)
APL=file

# Required if APL=upstash: Upstash Redis configuration for multi-tenant apps
# UPSTASH_URL=your-upstash-redis-url
# UPSTASH_TOKEN=your-upstash-redis-token

# Local development variables for Docker setup (usually not needed)
APP_IFRAME_BASE_URL=
APP_API_BASE_URL=